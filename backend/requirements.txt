# ClipForge — free/local Python dependencies
#
# Install with: pip install -r backend/requirements.txt
#
# Everything here is free and open-source. Nothing in this file requires an
# API key or a paid account.

# Real, free, local speech-to-text (default STT_PROVIDER=local).
# Runs on CPU by default (slower than a paid API, but $0). Set
# WHISPER_DEVICE=cuda in backend/.env if a GPU is available.
faster-whisper>=1.0.0

# Real face detection for 9:16 speaker-aware reframing.
opencv-python>=4.9.0
numpy>=1.26.0

# Real YouTube acquisition (also installable as a standalone CLI —
# having it as a Python package too makes `pip install -r requirements.txt`
# a genuine one-command setup for the whole free pipeline).
yt-dlp>=2024.8.6

{
  "name": "clipforge-backend",
  "version": "1.0.0",
  "description": "ClipForge API — job queue + AI pipeline orchestration for automatic YouTube-to-shorts clipping",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0"
  }
}
